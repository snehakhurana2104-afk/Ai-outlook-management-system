// ===========================================================
// responseRateRoutes.js
// ===========================================================

const express = require("express");

const {
  getResponseRate,
} = require(
  "../controllers/responseRateController"
);

const router =
  express.Router();

// -----------------------------------------------------------
// REAL RESPONSE RATE
// -----------------------------------------------------------

router.get(
  "/response-rate",
  getResponseRate
);

module.exports = router;// ===========================================================
// responseRateRoutes.js
// ===========================================================

const express = require("express");

const {
  getResponseRate,
} = require(
  "../controllers/responseRateController"
);

const router =
  express.Router();

// -----------------------------------------------------------
// REAL RESPONSE RATE
// -----------------------------------------------------------

router.get(
  "/response-rate",
  getResponseRate
);

module.exports = router;// ===========================================================
// responseRateRoutes.js
// ===========================================================

const express = require("express");

const {
  getResponseRate,
} = require(
  "../controllers/responseRateController"
);

const router =
  express.Router();

// -----------------------------------------------------------
// REAL RESPONSE RATE
// -----------------------------------------------------------

router.get(
  "/response-rate",
  getResponseRate
);

module.exports = router;